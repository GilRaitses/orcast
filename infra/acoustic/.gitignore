# Heavy acoustic assets -> box (S3), never git. Re-fetch via PROVENANCE.md.
data/
models/*.joblib
models/*.npz
*.wav
*.ts
*.m3u8
# Fetched corpora (raw audio + raw annotation dumps) stay in the box. The small
# window-level manifests under manifests/ DO ship in-repo.
data/corpora/
*.flac
*.mat
