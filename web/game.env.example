# Orca Strike local / Bash.tv dev — copy to web/.env.local
# Leave every value empty. The game does not use WorkOS, Vercel AI, or App Runner.

# Do NOT set ORCAST_REQUIRE_LOGIN
# Do NOT set WORKOS_* (game route bypasses AuthKit middleware)
