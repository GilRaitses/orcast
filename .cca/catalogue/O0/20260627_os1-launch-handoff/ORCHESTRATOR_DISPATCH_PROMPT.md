# Dispatch prompt, OS1 + beyond orchestrator

Paste the block below into the new thread to launch it. It hydrates from files, acks per the charter,
and stops for operator direction before acting.

---

You are O0 on the orcast open-science effort/detectability lane (OS1) and its "beyond". A prior
orchestrator built and MEASURED OS1 end-to-end; you inherit a closed, measured result and decide what
comes next. Do not promote anything. Do not commit without an explicit operator ask.

Hydrate, in order, from:
`.cca/catalogue/O0/20260627_os1-launch-handoff/HYDRATION_PACKET.md`
(start with `HANDOFF_CHARTER.md` in that same folder; then `OS1_BUILD_NOTE.md`,
`os1_skill_result.json`, and `STEP_LOG.md` under `.cca/catalogue/O0/20260627_open-science-integration/`).

Then, before any action, return the ack per `HANDOFF_CHARTER.md` §H:
- hydration confirmed + files read;
- the locked/measured items (§B) in your own words -- especially: detection-range calculator VALIDATED
  (sign bug fixed) and OrcaSound DSP pipeline PROVEN; ONC ambient proxy is a DEAD END (data geometry);
  OSF data is internal-only-licensed; OS1 is a MEASURED NO-GO on served skill (kappa sweep: mean CV
  deviance-skill +0.2776 baseline -> -0.1300 at full offset, monotonic decrease, no kappa beats
  baseline); no promotion (confidence 0.0); local-only/footgun rules;
- gate state in one line (confidence 0.0; OS1 measured NO-GO);
- your proposed "beyond" choice from §D (1 other-station confirmation, 2 fitted covariate, 3 better
  ambient/reframe, 4 CLOSE + bank physics for OS2/TB -- the prior orchestrator recommends 4), and
  whether the operator wants OS2 (Tekteksen/SIMRES, 11 net-new JJA days) picked up as the next lever;
- one risk.

Constraints: effective confidence stays 0.0 until a passing gate + recorded supervisor decision;
re-running the same noisy ambient cannot move the mean (only a different framing or a genuinely
co-located-ambient node could); read-only research + bounded local reruns; no production-store writes,
no deploys, no fit that writes, no promotion, no commit/`git add -A` without an explicit operator ask.

Stop after the ack and wait for the operator to choose the "beyond" path.
